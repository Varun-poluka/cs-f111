#include <stdio.h>
//printing values of a and b given 
int main()
{
    int a, b;//initialising a and b
    printf("enter the value of a");
    scanf("%d", &a);//reading value of a and storing it in the variable a 
    printf("enter the value of b");
    scanf("%d", &b);//reading the value of b and storing it in the variable b
    printf("a=%d\n", a);
    printf("b=%d\n", b);
    return 0;
}